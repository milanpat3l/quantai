# Streamlit secrets. On Streamlit Community Cloud, paste this (with your real
# token) into Settings -> Secrets. Locally you can instead use a .env file.
# NEVER commit the real token.
UPSTOX_ACCESS_TOKEN = "paste-your-analytics-token-here"
